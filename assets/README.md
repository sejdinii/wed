# Brand assets

`icon.png`, `adaptive-icon.png` and `splash-icon.png` are **generated placeholders**
(a gold "kapar coin" on brand green). If they are missing after cloning, regenerate:

```bash
python3 scripts/generate-assets.py
```

Replace with final brand assets before any store submission.
